- [x] 成功使用 `@napi-rs/cli` 构建基础扩展并在 JS 中加载 (Task 1)
- [x] 移除 `binding.gyp` 等 C++ 构建脚本 (Task 1)
- [x] 成功替换 `package.json` 中的 `install` 和 `build` 脚本以使用 Rust 构建工具 (Task 1)
- [x] 核心工具模块 (Config, Logger, Utils) 的功能在 Rust 中正确实现，与 C++ 的输出格式一致 (Task 2)
- [x] `start` 和 `configure` 接口可以在 JS 中正常被调用并返回预期结果 (Task 2)
- [x] 成功从 Rust 调用 V8 API 实现 CPU Profiler，并能导出合法的 CPU profile 格式 (Task 3)
- [x] Heap Profiler 可以在指定路径下正确 dump `heap snapshot` (Task 3)
- [x] GC Profiler 可以正确采集垃圾回收信息 (Task 3)
- [x] 进程在遇到 Fatal Error 时可以触发 Node Report 生成 (Task 4)
- [x] Logbypass 线程能够周期性采集 CPU/Memory/GC 统计信息并写入本地日志 (Task 5)
- [x] IPC socket (Unix/Win) 可以正常与 `xprofctl` 进行通信 (Task 5)
- [x] 所有 npm run test 跑通，所有暴露出来的 native APIs 的行为在功能和边界条件下均一致 (Task 6)
- [x] `src` 目录下的所有 C++ 源文件被完全删除并被 Rust `src` 替代 (Task 6)